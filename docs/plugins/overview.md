# Архитектура Плагинов

SORA поддерживает изолированные плагины. Они запускаются в процессе инициализации и общаются с оркестратором через NDJSON `stdout` / `stdin`.

Это позволяет писать плагины на чем угодно: Python, Go, Bash или Rust.
