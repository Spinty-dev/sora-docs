# Очередь отправки (TxQueue) & StealthEngine

`TxQueue` обеспечивает честное распределение пакетов (Fair drain) для инжекции. 

`StealthEngine` фильтрует `TxQueue`:
- Добавляет джиттер к инъекциям.
- Выполняет OUI-спуфинг (MAC-маскировка).
- Лимитирует Burst-TX для обхода WIDS-систем.
