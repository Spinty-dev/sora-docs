# Python Оркестрация (Управляющий Слой)

Слой Python отвечает за UI (через `textual`), бизнес-логику атак, работу с базой данных (SQLite) и запуск процессов-плагинов.

Основная задача слоя — принимать потоковые события от Rust и переключать состояния конечного автомата (FSM).
