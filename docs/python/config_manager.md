# Config Manager и Парсинг TOML

Единый источник истины — TOML-профили. Описание того, как:
- Загружаются конфигурации атак.
- Рендерится `hostapd.conf` на лету.
- Вызывается hot reloading для `EvilTwin` атак.
